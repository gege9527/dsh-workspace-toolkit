// Host half of dsh-workspace-toolkit.
// DSH already exposes session.openWorkspacePath, so no host API is needed.

export const name = 'workspace-toolkit'

export function apply() {
  // No server state to register.
}
